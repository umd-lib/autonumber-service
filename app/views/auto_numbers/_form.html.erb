<%= form_for(@auto_number) do |f| %>
  <% if @auto_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auto_number.errors.count, "error") %> prohibited this auto_number from being saved:</h2>

      <ul>
      <% @auto_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry_date %><br>
    <%= f.date_select :entry_date %>
  </div>
  <div class="field">
    <%= f.label :name_id %><br>
    <%= collection_select(:auto_number, :name_id, Name.all, :id, :initials) %>
  </div>
  <div class="field">
    <%= f.label :repository_id %><br>
    <%= collection_select(:auto_number, :repository_id, Repository.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
